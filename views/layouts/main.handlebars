<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Home</title>
</head>
<body>
    <h1>Lista de Produtos</h1>
    <ul>
        {{#each products}}
            <li>{{title}} - {{description}} - {{price}} - {{thumbnail}} - {{code}} - {{stock}}</li>
        {{/each}}
    </ul>
    <a href="../../realtimeproducts">Ver produtos em tempo real</a>
</body>
</html>
